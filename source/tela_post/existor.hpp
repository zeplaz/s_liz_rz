

// audio
//charctor?
//actor


#include<string>

#include <unordered_map>

#include <forward_list>

class label_map
{
	std::unordered_map<std::string,int> map;

	//sort list indexs?
	//

	std::vector<std::string> alpha_sorted_vec()
	{

		std::vector 

		return 
	} 

	std::forward_list<std::strings> f_list()
	{

	} 

};


class Lable_ID
{
	std::string id;
	//need to check for clash.
};

struct  existor
{

	Lable_ID id_lable;
	OBJ_ID   id_engine;

	std::string name;
	int ncopies;
	int ref_count;
	std::size_t size; 
	
}; 



